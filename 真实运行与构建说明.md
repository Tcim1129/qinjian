# 真实运行与构建说明

更新时间：2026-03-21

## 已完成验证

### 后端

- 测试命令：
  - `C:\Users\colour\Desktop\亲健\backend\venv\Scripts\python.exe -m pytest`
- 当前结果：
  - `11 passed`
- 启动冒烟：
  - `C:\Users\colour\Desktop\亲健\backend\venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8011`
  - `GET http://127.0.0.1:8011/api/health`
- 当前结果：
  - 返回 `{"status":"ok","app":"亲健 API"}`

### Uni App

- 依赖安装：
  - `npm install`
- H5 构建：
  - `npm run build:h5`
- 当前结果：
  - 已成功输出到 [dist\build\h5](C:\Users\colour\Desktop\亲健\qinjian-app\qingjian app\dist\build\h5)

## 本轮关键修复

- 小程序和 App 的 API 层补了统一超时、读请求重试、401 失效清理、错误归一化。
- App 工程的 `vite` 版本已对齐到 `2.9.14`，解决依赖树冲突。
- App 构建链新增 `sync:src` 同步步骤，避免 `uni` 旧版默认要求 `src/pages.json` 导致构建失败。
- 同步脚本位于 [sync-uni-src.ps1](C:\Users\colour\Desktop\亲健\qinjian-app\qingjian app\scripts\sync-uni-src.ps1)

## 当前启动方式

### 后端

1. 进入 [backend](C:\Users\colour\Desktop\亲健\backend)
2. 运行：

```powershell
C:\Users\colour\Desktop\亲健\backend\venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

### Uni App

1. 进入 [qingjian app](C:\Users\colour\Desktop\亲健\qinjian-app\qingjian app)
2. 首次安装依赖：

```powershell
npm install
```

3. 构建 H5：

```powershell
npm run build:h5
```

## 已知风险

- 当前后端环境使用 Python 3.14，会出现 `openai/_compat.py` 的兼容性警告。虽然本轮测试和启动都能通过，但正式部署更建议使用 Python 3.12 或 3.13。
- Uni App 当前依赖的是 alpha 版本工具链，构建可用，但后续最好安排一次依赖升级和回归测试。
- 小程序这轮主要完成了代码级和接口级稳定性增强，还没有做微信开发者工具里的真机回归。

## 建议的下一步

1. 跑一遍真实账号链路：登录、配对、打卡、生成报告、提交体检。
2. 在微信开发者工具里做一轮小程序真机回归。
3. 给后端补一个部署环境说明，统一 Python 和数据库版本。
