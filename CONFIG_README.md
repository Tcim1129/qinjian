# 小程序配置说明

## 需要复制的文件

1. 将根目录的 `project.config.template.json` 复制为 `project.config.json`
2. 将 `miniprogram/project.config.template.json` 复制为 `miniprogram/project.config.json`

## 需要替换的内容

- 把模板里的 `YOUR_APPID_HERE` 替换成你自己的微信小程序 `AppID`

## 提交注意

不要把真实 `AppID`、私有配置和本地调试信息提交到仓库。

建议保持这些文件只在本地存在：

- `project.config.json`
- `project.private.config.json`
- `miniprogram/project.config.json`

## 当前前端资源

小程序品牌图统一放在：

```text
miniprogram/images/brand-logo.jpg
```

## 版权

版权所有：心晴合伙人项目组 黄菁蓝、吴秀秀、叶笙尧、钟昊桐、郑梓滢
